# Stateboard Core

[English](README.md)

这里预留 Stateboard 的核心模型和同步逻辑。

计划包含：

- `ProjectState`
- `ProjectInventory`
- `TaskGranularity`
- `PriorityRubric`
- `IdempotentSyncPlan`

第一阶段不要急着写复杂框架。先从 `skills/stateboard-codex/` 的真实用法中抽象。
